---
"@klappay/checkout-kit": patch
---

Bump `@klappay/node` to `^3.4.2` and `@klappay/types` to `^3.5.2`. Both are doc-only patches (document the `409 idempotency_key_reused` error on `charges.create()`'s `idempotencyKey`) — no schema or code change.
